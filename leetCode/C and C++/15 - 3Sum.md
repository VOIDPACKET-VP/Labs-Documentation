---
layout: layouts/entry-detail.njk
title: 15 - 3Sum
date: 2026-08-21
category: Programming
platform: LeetCode
difficulty: Medium
tags:
  - Two-pointer
  - Loops
summary: Making Triplets
backLink: /writeups/
backLabel: Writeups
---
# Description 
Given an integer array nums, return all the triplets `[nums[i], nums[j], nums[k]]` such that `i != j`, `i != k`, and `j != k`, and `nums[i] + nums[j] + nums[k] == 0`.

Notice that the solution set must not contain duplicate triplets.

**Example 1:**
	**Input:** nums = [-1,0,1,2,-1,-4]
	**Output:** [[-1,-1,2],[-1,0,1]]
	**Explanation:** 
	nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0.
	nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0.
	nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0.
	The distinct triplets are [-1,0,1] and [-1,-1,2].
	Notice that the order of the output and the order of the triplets does not matter.

**Example 2:**
	**Input:** nums = [0,1,1]
	**Output:** []
	**Explanation:** The only possible triplet does not sum up to 0.

**Example 3:**
	**Input:** nums = [0,0,0]
	**Output:** [[0,0,0]]
	**Explanation:** The only possible triplet sums up to 0.

# Solution
- C++
```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        std::vector<std::vector<int>> triplets;
        std::sort(nums.begin(), nums.end());

        for (int i = 0; i < nums.size(); i++) {
            if (nums[i] > 0) break;
            if (i > 0 && nums[i] == nums[i - 1]) continue; // skip duplicates

            int start = i + 1;
            int end = nums.size() - 1;

            while (start < end) {
                int tripleSum = nums[i] + nums[start] + nums[end];
                if (tripleSum > 0) end--;
                else if (tripleSum < 0) start++;
                else {
                    triplets.push_back({nums[i], nums[start], nums[end]});
                    start++; end--;
                    // Skip duplicates at the left pointer
                    while (start < end && nums[start] == nums[start - 1]) {
                        start++;
                    }
                }
            }
        }
        return triplets;
    }
};
```

# Learned
It's all about Edge Cases with these programming challenges 